/*rander -v1.0  轻量级html模板引擎  By WinterT*/
(function ($) { $.fn.render = function (config) { var reg = new RegExp("\\[([^\\[\\]]*?)\\]", "igm"); var template = config.template; var list = ""; for (var i = 0; i < config.data.length; i++) { var item = config.data[i]; var temp = template.replace(reg, function (node, key) { if (item[key]) { return item[key] } else { return node } }); if (config.extend) { var extObj = config.extend(item); var temp = temp.replace(reg, function (node, key) { return extObj[0][key] }) } list += temp } if (config.head) { list = $(config.head).parent().html() + list } $(this).html(list) } })(jQuery);
